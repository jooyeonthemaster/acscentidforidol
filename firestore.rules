rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 임시로 모든 읽기/쓰기 허용 (개발용)
    // 프로덕션에서는 적절한 인증 규칙 필요
    match /{document=**} {
      allow read, write: if true;
    }
  }
}